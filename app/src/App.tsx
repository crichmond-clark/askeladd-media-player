import { MediaControls } from "./components/MediaControls";

function App() {
  return (
    <main className="bg-grey-base h-screen flex flex-col">
      <MediaControls />
    </main>
  );
}

export default App;
